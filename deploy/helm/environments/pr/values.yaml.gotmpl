ingress:
  enabled: true
  hosts:
    - host: {{ requiredEnv "NS" }}.rdx-works-main.extratools.works
      paths:
      - path: /
        backend:
          serviceName: signaling-server
          servicePort: 4000
  tls:
    - secretName: self-signed-tls
      hosts:
        - {{ requiredEnv "NS" }}.rdx-works-main.extratools.works
